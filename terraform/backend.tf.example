# Copy this file to backend.tf and fill in your values.
# backend.tf is gitignored — it contains your project-specific state bucket.

terraform {
  backend "gcs" {
    bucket = "YOUR_BUCKET_NAME"   # e.g. "gmail-organizer-tf-state"
    prefix = "terraform/state"
  }
}
